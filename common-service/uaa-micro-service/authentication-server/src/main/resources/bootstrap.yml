# Springboot先加载bootstrap.yml，后加载application.yml，最终两份配置合并到一块开始初始化Spring容器
spring:
  application:
    name: authentication-service # 服务名称
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848